const express = require('express');
const router = express.Router();

// Placeholder for shared notifications routes
router.get('/', (req, res) => {
    res.json({
        success: true,
        message: 'Shared notifications functionality coming soon'
    });
});

module.exports = router;
