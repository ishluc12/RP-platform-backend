const express = require('express');
const router = express.Router();

// Placeholder for shared forums routes
router.get('/', (req, res) => {
    res.json({
        success: true,
        message: 'Shared forums functionality coming soon'
    });
});

module.exports = router;
